{%- set command_ip_map = {} -%}
{%- set cmd = "ED-PID:%s:default_user:CTAG::default_password,%s;" | format(TID,new_password) -%}
{%- set dummy = command_ip_map.update({cmd: {}}) -%}
{{- command_ip_map -}}