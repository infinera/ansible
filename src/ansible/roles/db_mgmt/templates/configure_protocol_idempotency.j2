{%- if existing_protocol_res["RETRIEVE DEFAULT(S) SECURITY ATTRIBUTES"].result["Component-0"].Parameters["FXFRSSH"] == "false" -%}
    {%- if db_configs.backup.primary_ftp_server.protocol == "ftp" -%}
        {{- false -}}
    {%- else -%}
        {{- true -}}
    {%- endif -%}
{%- else -%}
    {%- if db_configs.backup.primary_ftp_server.protocol == "sftp" -%}
        {{- false -}}
    {%- else -%}
        {{- true -}}
    {%- endif -%}
{%- endif -%}