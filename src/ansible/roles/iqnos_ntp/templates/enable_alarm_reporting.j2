{%- set command_ip_map = {} -%}
{%- for identifier in enable_arc_configs.identifiers -%}
    {%- set cmd = "RLS-ARC-NTP::%s:CTAG::;" | format(identifier) -%}
    {%- set dummy = command_ip_map.update({cmd: {"ARC": "RLS"}}) -%}
{%- endfor -%}
{{- command_ip_map -}}